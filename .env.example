PORT=3090
NODE_ENV=development
LOG_LEVEL=info

# Poll all services every 30 seconds
HEALTH_POLL_INTERVAL_MS=30000
HEALTH_CACHE_TTL_SECONDS=120
HEALTH_RETRY_ATTEMPTS=3
HEALTH_REQUEST_TIMEOUT_MS=5000
HEALTH_RETRY_BACKOFF_MS=1000

# Redirect when any monitored service is down
MAINTENANCE_PORTAL_URL=https://aicert.lms.com
ALLOWED_ORIGINS=http://localhost:3002,https://devdashboard.proctor365.ai

# Optional: protect POST /system/check-now (send as x-health-check-key header)
HEALTH_CHECK_API_KEY=

# Email alerts via SendGrid when a monitored API goes DOWN or recovers to UP
ALERT_EMAIL_ENABLED=false
ALERT_EMAIL_TO=developer1@example.com,developer2@example.com
# Wait this long after the first DOWN/UP before sending, so multiple events of the same type merge into one email
ALERT_EMAIL_BATCH_WINDOW_MS=30000
SENDGRID_API_KEY=
EMAIL_FROM_NAME=Proctor365 Sandbox
EMAIL_FROM_ADDRESS=customerservicedev@mail.proctor365.ai

# Optional: route alerts for a specific service to different developers
# ALERT_EMAIL_AUTH_SERVICE=auth-team@example.com
# ALERT_EMAIL_COMMUNICATION_SERVICE=comms-team@example.com

# Redis (optional — falls back to in-memory cache if REDIS_HOST is empty)
# IMPORTANT: local and DEV must not share the same cache key.
# Set HEALTH_CACHE_NAMESPACE=dev on the DEV server, HEALTH_CACHE_NAMESPACE=local on your laptop.
HEALTH_CACHE_NAMESPACE=local
REDIS_HOST=
REDIS_PORT=6379
REDIS_USERNAME=
REDIS_PASSWORD=

# Microservice base URLs (adjust for your environment)
AUTH_SERVICE_URL=http://localhost:3000
COMMUNICATION_SERVICE_URL=http://localhost:3001
ADMIN_MANAGEMENT_URL=http://localhost:3003
PROCTORING_BACKEND_URL=http://localhost:3030
RABBITMQ_CONSUMER_URL=http://localhost:3004
IO_HANDLER_URL=http://localhost:3010
WEBRTC_SERVER_URL=http://localhost:4000
ADMIN_PORTAL_URL=http://localhost:4012
CANDIDATE_PORTAL_URL=http://localhost:4005
VIDEO_SERVICE_URL=https://video.proctor365.ai
AUDIO_SERVICE_URL=https://audio.proctor365.ai
